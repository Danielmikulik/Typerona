using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.IO;
using System.Linq;

public class TextListLoader : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //string filePath = Application.streamingAssetsPath + "/WordList" + ".txt";

        //string[] fileLines = File.ReadAllLines(filePath).ToArray();

        //WordGenerator.SetWordList(fileLines);
    }
}
